!!! To do list !!!

Add encryption method that takes salt as well
Add decryption method that takes salt as well

Update DNS with gethostentry instead of dns.resolve

Watcher event instead of writing to console

Filemanager, add lock and unlock files